setuptools
requests
wheel
faster-whisper
sounddevice
numpy

# AI Processing dependencies
PyYAML>=6.0
python-dotenv>=1.0.0

# TTS dependencies
chatterbox-tts
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.0

# Optional: for auto-type feature (Windows/macOS/Linux X11)
# Linux Wayland users should install wtype or ydotool instead
pyautogui
